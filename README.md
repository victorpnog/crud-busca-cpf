- Acessar via: https://crud-busca-cpf-ss9l.onrender.com

- Explicação da função buscar dados:
A função buscarDados utiliza uma requisição GET para o endpoint da API passando o CPF como uma Query String (ex: /pessoas?cpf=VALOR). O JSON Server filtra o banco de dados e retorna um array com os registros correspondentes.
